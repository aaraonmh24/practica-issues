# Cómo contribuir

- Rama base: `dev`. Nunca hacer push directo a `main` ni a `dev`.
- Una rama por issue: `fix/<numero>-<slug>`.
- Commits: `fix: <descripción>` y `Closes #<n>` en el cuerpo.
- Antes de abrir el PR, `./compilar.sh` debe pasar.
- Los PR deben resolver solo lo que pide el issue.
